[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Test markers
markers =
    integration: E2E integration tests with real LLM and /call endpoint (run locally with DISABLE_TG=true)
    slow: Tests that take longer than 5 seconds
    llm_judge: Tests that use LLM-as-judge validation
